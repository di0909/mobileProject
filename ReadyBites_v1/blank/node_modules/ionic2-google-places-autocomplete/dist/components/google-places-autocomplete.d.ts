import { EventEmitter } from '@angular/core';
import { Http } from '@angular/http';
import { Observable } from 'rxjs/Rx';
import 'rxjs/add/operator/map';
export declare class GooglePlacesAutocompleteComponent {
    http: Http;
    callback: EventEmitter<any>;
    placeholder: string;
    types: string;
    type: string;
    key: string;
    offset: string;
    location: string;
    radius: string;
    language: string;
    components: string;
    strictbounds: string;
    locals: any[];
    constructor(http: Http);
    autocomplete(input: string): Observable<any>;
    getLocals(ev: any): void;
    detail(item: any): void;
}
